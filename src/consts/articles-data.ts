import { ResourceCard } from "@/types/resource";

import { HEALTH_WELLNESS_ARTICLES } from "./health-wellness-articles";

export const ARTICLES_BY_RESOURCE: Record<string, ResourceCard[]> = {
  "salud-y-bienestar": HEALTH_WELLNESS_ARTICLES,
};
