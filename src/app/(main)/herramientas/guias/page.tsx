import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Guías",
  alternates: {
    canonical: "/herramientas/guias",
  },
};

const GuidesPage = () => {
  return null;
};

export default GuidesPage;
