import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Enlaces",
  alternates: {
    canonical: "/adicionales/enlaces",
  },
};

const EnlacesPage = () => {
  return null;
};

export default EnlacesPage;
