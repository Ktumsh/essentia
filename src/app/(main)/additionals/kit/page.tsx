import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Botiquín",
  alternates: {
    canonical: "/additionals/kit",
  },
};

export default function KitPage() {
  return null;
}
