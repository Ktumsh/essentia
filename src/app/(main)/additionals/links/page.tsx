import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Enlaces",
  alternates: {
    canonical: "/additionals/links",
  },
};

const EnlacesPage = () => {
  return null;
};

export default EnlacesPage;
