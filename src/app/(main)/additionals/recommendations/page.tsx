import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Recomendaciones",
  alternates: {
    canonical: "/additionals/recommendations",
  },
};

const RecomendacionesPage = () => {
  return null;
};

export default RecomendacionesPage;
