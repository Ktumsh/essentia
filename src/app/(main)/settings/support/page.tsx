import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Configuración - Ayuda y soporte",
  alternates: {
    canonical: "/settings/support",
  },
};

const SupportSettingsPage = () => {
  return null;
};

export default SupportSettingsPage;
